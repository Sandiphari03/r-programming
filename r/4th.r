#array

{
  n=as.integer(readline('enter size of array :'))
  for(i in 1:n)
  {
    name[i]=readline()
  }
name
}
{
  n=as.integer(readline('No. of students :'))
  Roll=c()
  Name=c()
  CGPA=c()
  for (i in 1: n )
    
  
}
